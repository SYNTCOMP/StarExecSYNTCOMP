#!/bin/bash
# This configuration allows to synthesize a controller.
# $1 contains the input filename (the name of the AIGER-file).
LTL=$(syfco $1 -f ltlxba -m fully)
IN=$(syfco $1 -f ltlxba -m fully)
OUT=$(syfco $1 -f ltlxba -m fully)
ltlsynt --formula="$LTL" --ins="$IN" --outs="$OUT" --aiger

#!/bin/bash
# This configuration allows to check (un)realizability only
# $1 contains the input filename (the name of the AIGER-file).
DEMIURGETP=./third-party
COMMAND="./demiurge --in=$1 --real_only --print=EW"
$COMMAND
exit

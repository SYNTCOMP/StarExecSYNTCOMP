#!/bin/bash
# This configuration allows to synthesize a controller.
# Additionally, it outputs the winning region as a certificate
# of correctness.
# $1 contains the input filename (the name of the AIGER-file).
COMMAND="./abssynthe $1 -o result.aag -w wregion.aag"
$COMMAND
res=$?
if [[ $res == 10 ]]; then
    echo "REALIZABLE"
    cat "result.aag"
    echo "WINNING_REGION"
    cat "wregion.aag"
else
    echo "UNREALIZABLE"
fi
exit $res
